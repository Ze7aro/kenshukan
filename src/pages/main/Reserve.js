import { Stack, Button } from "@chakra-ui/react";
import React from "react";

const Reserve = () => {
  return (
    <Stack>
      <Button isDisabled>Reserva tus asiento</Button>
    </Stack>
  );
};

export default Reserve;
